import 'package:flutter/material.dart';

class CountrySearchUseMysiteWidget extends StatelessWidget {
  const CountrySearchUseMysiteWidget({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 44,
      child: Placeholder(),
    );
  }
}
