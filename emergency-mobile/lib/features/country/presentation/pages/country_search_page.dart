import 'package:flutter/material.dart';

class CountrySearchPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('country search page'),),
    );
  }
}